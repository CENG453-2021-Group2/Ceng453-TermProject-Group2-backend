package group2.monopoly.leaderboard;

import org.springframework.boot.CommandLineRunner;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;


@Configuration
public class LeaderboardRecordConfig {

    /*
    @Bean
    CommandLineRunner commandLineRunner(LeaderboardRecordRepository LeaderboardRecordRepository) {
        return args -> {
            LeaderboardRecord kobe = new LeaderboardRecord(
                    1L,
                    20L
            );
            LeaderboardRecord jordan = new LeaderboardRecord(
                    2L,
                    10L
            );


            LeaderboardRecordRepository.saveAll(java.util.Arrays.asList(kobe, jordan));
        };
    }

     */
}
